import { ApplicationReviewer } from './application-reviewer';

describe('ApplicationReviewer', () => {
  it('should create an instance', () => {
    expect(new ApplicationReviewer()).toBeTruthy();
  });
});
