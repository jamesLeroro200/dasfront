import { RecognizedStandards } from './recognized-standards';

describe('RecognizedStandards', () => {
  it('should create an instance', () => {
    expect(new RecognizedStandards()).toBeTruthy();
  });
});
